# Non-consensus assessments

This directory stores evidence-grounded research assessments as Wiki truth
source pages. Files beginning with `_` are documentation and are not indexed as
entities.

Valid results are:

- `supported-consensus`
- `contested`
- `insufficient-evidence`

`insufficient-evidence` is a valid conclusion. Never create a contradiction to
satisfy a completion threshold.
